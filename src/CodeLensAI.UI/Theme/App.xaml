<Application x:Class="CodeLensAI.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CodeLensAI.UI"
             StartupUri="/Views/MainWindow.xaml">
    <Application.Resources>
        <Color x:Key="BackgroundColor">#1E1E1E</Color>
        <Color x:Key="SurfaceColor">#2D2D30</Color>
        <Color x:Key="BorderColor">#3F3F46</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="AccentColor">#007ACC</Color>
        <Color x:Key="AccentHoverColor">#1C97EA</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}" />

        <!-- Text color is NOT set via an implicit TextBlock style on purpose: a style setter
             outranks property inheritance, which would pin every unstyled TextBlock to one brush
             and stop tree items from inheriting their selection foreground. Text instead inherits
             the Window's Foreground below (the same mutable TextBrush instance the theme code
             updates in place). -->
        <Style TargetType="{x:Type Window}">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </Application.Resources>
</Application>
