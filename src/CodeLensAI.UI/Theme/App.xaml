<Application x:Class="CodeLensAI.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="/Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Startup palette; ThemeManager retargets every brush when the theme changes. -->
                <ResourceDictionary Source="/Theme/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Semantic brushes. One per *Color key in the theme dictionaries; ThemeManager
                 mutates these in place (or replaces them when frozen). No hex values here. -->
            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
            <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
            <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
            <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource PrimaryHoverColor}" />
            <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}" />
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
            <SolidColorBrush x:Key="TreeSelectionBrush" Color="{StaticResource TreeSelectionColor}" />
            <SolidColorBrush x:Key="HeaderBrush" Color="{StaticResource HeaderColor}" />
            <SolidColorBrush x:Key="StatusBarBrush" Color="{StaticResource StatusBarColor}" />
            <SolidColorBrush x:Key="SidebarBrush" Color="{StaticResource SidebarColor}" />
            <SolidColorBrush x:Key="HoverOverlayBrush" Color="{StaticResource HoverOverlayColor}" />
            <SolidColorBrush x:Key="PressedOverlayBrush" Color="{StaticResource PressedOverlayColor}" />
            <SolidColorBrush x:Key="ScrollThumbBrush" Color="{StaticResource ScrollThumbColor}" />
            <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="{StaticResource ScrollThumbHoverColor}" />
            <SolidColorBrush x:Key="ShadowBrush" Color="{StaticResource ShadowColor}" />

            <!-- Typography -->
            <FontFamily x:Key="UiFont">Segoe UI</FontFamily>
            <FontFamily x:Key="CodeFont">Cascadia Code, Consolas, Courier New</FontFamily>
            <FontFamily x:Key="IconFont">Segoe MDL2 Assets</FontFamily>

            <!-- Text color is NOT set via an implicit TextBlock style on purpose: a style setter
                 outranks property inheritance, which would pin every unstyled TextBlock to one
                 brush and stop tree items from inheriting their selection foreground. Text
                 inherits the Window's Foreground below. DynamicResource keeps the style working
                 even when ThemeManager has to replace (not mutate) a frozen brush. -->
            <Style TargetType="{x:Type Window}">
                <Setter Property="Background" Value="{DynamicResource BackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
                <Setter Property="FontSize" Value="13" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
